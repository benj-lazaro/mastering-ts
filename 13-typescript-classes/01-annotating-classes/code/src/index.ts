// Annontating Classes in TypeScript

class Player {
  first: string;
  last: string;

  constructor(first: string, last: string) {
    this.first = first;
    this.last = last;
  }
}

const wick = new Player("John", "Wick");
