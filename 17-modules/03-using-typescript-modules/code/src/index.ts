// Using TypeScript Modules

import { add, sample } from "./utils.js";

const sampleArrayElement = sample([1, 2, 3, 4, 5]);
console.log(`Sample Element: ${sampleArrayElement}`);

const sum = add(12, 34);
console.log(`Sum: ${sum}`);
