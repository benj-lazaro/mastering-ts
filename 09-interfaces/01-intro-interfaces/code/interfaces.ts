// Introducing Interfaces

// Type Alias
type TypePoint = {
  x: number;
  y: number;
};

// Interface
interface InterfacePoint {
  x: number;
  y: number;
}

const coord1: TypePoint = { x: 31, y: 41 };

const coord2: InterfacePoint = { x: 12, y: 22 };
