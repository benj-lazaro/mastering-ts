// TypeScript's Non-Null Assertion Operator

// Using with a non-null assertion operator
const btn = document.getElementById("btn")!;

// Using with an optional chaining operator
// btn?.addEventListener("click", () => alert("Clicked"));

btn.addEventListener("click", () => alert("Clicked"));
